# All Buttons Inventory

The following list catalogs every `<Button>` component usage across the application, categorized by module and page. It highlights purpose (label/icon) and styling variants (e.g., `outline`, `destructive`, `ghost`, `link`).

## Administration Module
- **Activity Logs (`Admin/ActivityLogs/Index.vue`)**
  - Clear Filters (`variant="outline"`)
  - Close View Log
  - Row Actions (View mapping)
- **Backup (`Admin/Backup/Index.vue`)**
  - Run Backup
  - Select File (`variant="outline"`)
  - Cancel / Delete (`variant="outline"`, `variant="destructive"`)
  - Row Actions (Download, Delete)
- **Calendar (`Admin/Calendar/Index.vue`)**
  - Add Holiday / Create Modal
  - Close / Save Changes (Modals)
  - Close / Delete Holiday
  - Close Event Details
  - Row Actions (Edit, Delete)
- **Leave (`Admin/Leave/Index.vue`)**
  - View Leave ??? (`variant="outline"`)
- **Notices (`Admin/Notices/Index.vue`, `Create.vue`, `Edit.vue`)**
  - Create Notice
  - Close, Edit Notice
  - Cancel / Delete Notice (`variant="outline"`, `variant="destructive"`)
  - Back to list (Create/Edit: `variant="outline"`)
  - Save Notice / Update
  - Cancel (Forms: `variant="outline"`)
  - Row Actions (View, Edit, Delete)
- **Notifications (`Admin/Notifications/Index.vue`)**
  - Mark as Read
- **PDS (`Admin/PDS/Index.vue`)**
  - Go to HR PDS ??? (`variant="outline"`)
- **Profile (`Admin/Profile/Index.vue`)**
  - Edit Profile, Change Password, Activity Logs
  - Delete Account (`variant="outline"`, red text styling)
  - Save Changes / Cancel (`variant="outline"`)
  - Change Password / Cancel (`variant="outline"`)
  - Delete Account Confirm (`variant="destructive"`) / Cancel
  - Close (Activity Modal)
- **Users (`Admin/Users/Index.vue`)**
  - Add New User
  - Clear Filters (`variant="outline"`)
  - Create User / Cancel
  - Save changes / Cancel
  - Close View, Edit User
  - Cancel / Delete Confirm
  - Row Actions

## HR Module
- **Calendar (`HR/Calendar/Index.vue`)**
  - View Event Details (Row Actions)
  - Close Modals (`variant="outline"`)
- **Leave (`HR/Leave/Index.vue`, `LeaveCredits/Index.vue`)**
  - Approve / Reject (`variant="destructive"`)
  - Submit / Cancel
  - Save / Cancel
  - Delete Confirm / Cancel (`variant="destructive"`)
  - Row Actions (View, Approve, Reject, Edit, Delete)
  - View Leave Credits
- **Notices (`HR/Notices/Index.vue`, `Create.vue`, `Edit.vue`)**
  - Create Notice
  - Close, Edit Notice
  - Cancel / Delete Notice (`variant="outline"`, `variant="destructive"`)
  - Back to list (Create/Edit: `variant="outline"`)
  - Save Notice / Update
  - Cancel (Forms: `variant="outline"`)
  - Row Actions
- **Notifications (`HR/Notifications/Index.vue`)**
  - Mark as Read
- **PDS (`HR/PDS/Index.vue`, `Preview.vue`)**
  - Approve / Reject (`variant="destructive"`)
  - Close Preview Modal (`variant="outline"`)
  - Row Actions (View, Approve, Reject)
- **Profile (`HR/Profile/Index.vue`)**
  - Edit Profile, Change Password, Activity Logs
  - Delete Account (`variant="outline"`, red text styling)
  - Save Changes / Cancel (`variant="outline"`)
  - Change Password / Cancel (`variant="outline"`)
  - Delete Account Confirm (`variant="destructive"`) / Cancel
  - Close (Activity Modal)
- **Training (`HR/Training/Index.vue`)**
  - Add Training
  - Clear Filters (`variant="outline"`)
  - Create / Cancel
  - Save / Cancel
  - Close / Edit
  - Delete Confirm / Cancel (`variant="destructive"`)
  - Row Actions
- **Users (`HR/Users/Index.vue`)**
  - Clear Filters (`variant="outline"`)
  - Close View
  - Save changes / Cancel
  - Delete Confirm / Cancel
  - Row Actions

## Employee Module
- **Calendar (`Employee/Calendar/Index.vue`)**
  - Export CSV (`variant="outline"`)
  - Close Modals (`variant="outline"`)
- **Dashboard (`Employee/Dashboard.vue`)**
  - Quick Links (4 buttons, `variant="link"`)
- **Leave (`Employee/Leave/Index.vue`)**
  - Apply for Leave (Primary layout `default`, Zero-state `variant="link"`)
  - Clear Filters (`variant="outline"`)
  - Submit / Cancel
  - Save changes / Cancel
  - Close View
  - Row Actions
- **PDS (`Employee/PDS/Index.vue`, `Preview.vue`)**
  - Tab controls for C1, C2, C3, C4 (Native `<button>` tags)
  - Add Record for multiple sections: Children, Education, CSC, Work, Voluntary, Training, Other Info (`variant="outline"`, `size="sm"`)
  - Delete row buttons for the above sections (Trash Icon: `variant="ghost"`, `size="icon"`, `text-destructive`)
  - Save Draft (`variant="outline"`)
  - Review & Submit triggers
  - Yes, submit / Cancel
  - View Preview (`Preview.vue`)
- **Profile (`Employee/Profile/Index.vue`)**
  - Edit Profile, Change Password, Activity Logs
  - Delete Account (`variant="outline"`, red text styling)
  - Save Changes / Cancel (`variant="outline"`)
  - Change Password / Cancel (`variant="outline"`)
  - Delete Account Confirm (`variant="destructive"`) / Cancel
  - Close (Activity Modal)
- **Training (`Employee/Training/Index.vue`)**
  - Add Training (Primary layout `default`, Zero-state `variant="link"`)
  - Clear Filters (`variant="outline"`)
  - Add / Cancel
  - Save changes / Cancel
  - Close View
  - Row Actions

## Auth & Settings Modules
- **Authentication Pages (`auth/`)**
  - Register, Login, Forgot Password, Reset Password, Confirm Password
  - Two Factor Challenge
  - Verify Email (`variant="secondary"`)
- **General Settings (`settings/`)**
  - Edit Profile Data (Save)
  - Password Settings (Update)
  - Two-Factor Auth (Enable, Disable: `variant="destructive"`, Regenerate Recovery Codes)
- **Global Notifications (`Notifications/Index.vue`)**
  - Mark as Read

## Global Components & Layouts
- **Header & Sidebar (`AppHeader.vue`, `AppSidebarHeader.vue`, `SidebarTrigger.vue`, `SidebarRail.vue`)**
  - Sidebar toggles (`variant="ghost"`, `size="icon"`)
  - User menu dropdown triggers
- **Shared Modals & Utilities (`DeleteUser.vue`, `SettingsContent.vue`, `AppearanceTabs.vue`)**
  - Delete User Confirm (`variant="destructive"`)
  - Theme toggles & Native tab `<button>` controls
- **Auth Components (`TwoFactorRecoveryCodes.vue`, `TwoFactorSetupModal.vue`, `auth/PasswordInput.vue`)**
  - 2FA Modals (Next Step, Confirm, Cancel)
  - Password Visibility toggles (Native `<button>`)
- **Layout Files (`layouts/auth/AuthSplitLayout.vue`, `layouts/settings/Layout.vue`)**
  - Mobile sheet triggers & native theme toggles
